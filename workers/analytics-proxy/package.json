{
  "name": "analytics-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker that proxies the Google Analytics 4 Data API for spiteware.ai/analytics.html",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev --port 8788 --inspector-port 9230",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.135.0"
  }
}
