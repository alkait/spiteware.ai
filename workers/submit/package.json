{
  "name": "submit",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker that turns the form on spiteware.ai/submit.html into one email to hello@spiteware.ai",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev --port 8789 --inspector-port 9231",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.135.0"
  }
}
