{
  "name": "submit",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker that turns the submit and contact forms on spiteware.ai into one email each",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev --port 8789 --inspector-port 9231",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.135.0"
  }
}
